## 1. 依赖契约与启动校验

- [x] 1.1 将 `coacd`、`python-fcl` 以及高精度 SDF 构建所需依赖调整为正式运行依赖，并移除“缺失原生依赖仍可运行”的安装语义
- [x] 1.2 在 CLI / 服务初始化阶段增加依赖自检，缺少关键依赖时直接失败并返回明确诊断
- [x] 1.3 删除 `require_native_coacd`、`require_native_fcl` 等为回退路径服务的配置与诊断码

## 2. 高精度 SDF 预处理

- [x] 2.1 增加面向高精度 SDF 的障碍物网格校验，覆盖闭合性、法向一致性、退化三角形与单位/坐标系合法性
- [x] 2.2 基于原始三角网格实现高精度体素化 SDF 构建，输出 `origin`、`spacing`、`shape`、`values` 与符号判定元数据
- [x] 2.3 将体素边长或等价分辨率、padding、符号判定方式纳入缓存键与预处理工件元数据
- [x] 2.4 移除当前基于局部 AABB / 凸块盒集合生成与查询 SDF 的实现

## 3. 精确检测与复检后端收紧

- [x] 3.1 删除 CoACD 缺失时退化为单凸块的实现，确保凸分解失败直接中断预处理
- [x] 3.2 删除 `python-fcl` 缺失时退化为 AABB 碰撞的实现，确保 narrowphase 与复检只走 FCL
- [x] 3.3 统一检测与复检的工件装载和错误语义，缺少正式几何工件时返回 `failed`

## 4. 自动修正链路对齐

- [x] 4.1 将优化阶段的距离查询切换为正式体素化网格 SDF 的连续采样，不再读取局部盒体集合
- [x] 4.2 在优化结果中记录 SDF 缓存键、体素精度参数与求解参数，确保结果可复现
- [x] 4.3 当障碍物缺少正式 SDF 工件时直接失败，不构造临时近似 SDF 或其他替代损失

## 5. 测试补强

- [x] 5.1 补充依赖缺失即失败的单元测试，验证系统不会进入近似回退路径
- [x] 5.2 补充高精度 SDF 预处理测试，验证工件结构、缓存键和输入拒绝逻辑
- [x] 5.3 补充精确检测与精确复检测试，验证系统不会使用单凸块或 AABB 近似替代正式结果
- [x] 5.4 补充自动修正测试，验证优化阶段使用正式 SDF 工件并返回可追溯元数据

## 6. 文档与规格同步

- [x] 6.1 更新 README，移除“兼容缺依赖环境”和“局部 AABB SDF”表述，明确项目要求高精度 SDF 与正式原生依赖
- [x] 6.2 同步更新 OpenSpec capability spec、设计文档和安装说明，确保文档、任务与实现目标一致
